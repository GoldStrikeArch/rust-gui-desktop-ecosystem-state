# Iteration 5 rows — "Windows" (SPEC-9) and "Ledger" (SPEC-10)

Raw per-framework rows as reported by each build agent, macOS 26.5.2 /
M4 Pro / rustc 1.96.1, 2026-08-30. Build times were measured while up to
six sibling builds ran concurrently, so treat them as upper bounds; the
serial `./measure.sh --round iter5` pass is the canonical number (ran 2026-08-30: `measurements/reruns/20260830T193713Z/results-iter5.csv`; all 22 apps survived the launch check). Ratings:
built-in / assembled / hand-rolled / not-achievable. Evidence in each
`apps/<fw>-<spec>/evidence/` (screenshots, `log.txt`, `selftest-log.txt`).

## dioxus (desktop, =0.7.9)

windows: build 35.0 s · 6,477,664 B · 291 crates · LoC 1065 (app 807 + platform 250 + main 8) · selftest 13/13
- second window built-in · modal **hand-rolled** (child window + click-swallowing scrim; tao exposes no modality) · parent blocked hand-rolled (synthetic-input) · focus return assembled · shared state **built-in** · ping/pong built-in · theme-all-windows built-in (paint lags on unfocused windows) · parenting **built-in** (tao `with_parent_window` → `addChildWindow`) · close veto **hand-rolled** (no veto in 0.7.9) · quit semantics assembled · native confirm assembled (rfd) · persistence assembled (child position overridden by macOS) · multi-monitor not-verified · shortcuts mixed
- window model: each window is its own `VirtualDom`; a `Signal` passed via `VirtualDom::new(..).with_root_context(shared)` is a generational-box handle, so reads subscribe the reading window and writes wake every subscriber through its own scheduler — real one-source-of-truth across windows with no channel. `GlobalSignal` resolves per runtime: each window silently gets its own copy.

ledger: build 54.4 s · 6,424,992 B · 293 crates · LoC 1000 (app 935 + platform 57 + main 8) · selftest 26/26
- numeric field hand-rolled · locale hand-rolled · decimal alignment **built-in** (`font-variant-numeric: tabular-nums`) · validation assembled · dropdown built-in · date built-in · slider↔field assembled · tab order built-in · Enter-moves-down assembled · undo built-in (field) + hand-rolled (form) · live totals built-in · TSV assembled (arboard, ⌘⇧C/⌘⇧V) · a11y **built-in, dump verified** (aria-label) · IME not-verified
- traps: fast typing into a controlled input drops characters (`1234.5` → `12.5` at ~30 ms/char; each keystroke round-trips DOM→IPC→VDOM→diff→DOM); rejecting a keystroke needs an element remount (`key:` bump); occluded window's `oninput` never reaches Rust (iteration-4 freeze re-confirmed).
- native port surface (platform.rs): windows — `launch, register, open_or_focus, is_open, open_count, close_key, close_self, focus, bounds, set_bounds, scale_factor, confirm, use_close_guard, quit` (+ selftest/place/on_top); ledger — `launch, clipboard_write, clipboard_read` (+ selftest/place).

## xilem (=0.4.0)

windows: build 27.6 s · 11,522,512 B (9,329,528 stripped) · 443 lock entries · LoC 1518 (main 1054 + shell 464; ~460 is a winit event-loop embedding) · deps masonry_winit =0.4.0, rfd =0.15.4, serde/serde_json · selftest 23/23
- second window built-in (singleton focus assembled) · modality **hand-rolled**; OS-modal only via `rfd::set_parent` · parent blocked hand-rolled (40 BLOCKED log lines, 6 rows before/after) · focus return assembled · shared state **built-in** · ping built-in / pong-timer assembled · theme-all-windows built-in · parenting **not-achievable** (`with_owner_window` is `#[cfg(windows)]`-only; only `AlwaysOnTop`) · close veto assembled · quit semantics built-in · native confirm assembled · persistence assembled · multi-monitor not-verified · shortcuts hand-rolled
- window model: a window is a **view**; one `AppState` serves N windows, so bidirectional inspector↔list is zero code. Geometry/position are initial-only (later changes only log a warning), so persistence/focus need the `masonry_winit` driver escape hatch (xilem does not re-export `MasonryState`/`AppDriver`/`DriverCtx`). Not forwarding `on_close_requested` IS the veto. `WindowLevel::AlwaysOnTop` lands at `CGWindowLayer 3`, invisible to `scripts/window-count.swift`. An unbundled binary's `NSAlert` is hosted out-of-process and survives SIGTERM as a ghost dialog unless `set_parent` is used.

ledger: build 29.9 s · 11,611,792 B (9,360,640 stripped) · LoC 1821 (main 630 · model 477 · shell 489 · views 225) · deps masonry_winit, rust_decimal =1.39.0, arboard =3.6.1 · selftest 13/13
- numeric field hand-rolled · locale hand-rolled · tabular figures **hand-rolled** (`StyleProperty::FontFeatures` reachable only by re-implementing the label view; alignment done with a two-box split) · validation assembled · dropdown hand-rolled · date mask hand-rolled · slider↔field assembled · tab order **built-in** · Enter-moves-down hand-rolled · undo field-level not-achievable / form-level hand-rolled · live totals built-in · TSV assembled (⌘⇧C/V) · a11y **partial** (61 field values in the tree, no names) · IME by-construction
- trap: masonry builds the AccessKit tree lazily — a one-shot AX query returns only title-bar elements and would wrongly read as "no a11y tree". The date mask corrupts an already-filled cell (`text_input` has no caret/selection API).

## gpui (=0.2.2)

windows: cold build 1 m 31 s · 5,720,240 B · LoC 1422 (1194 + 228 self-test) · zero helper crates · selftest 16/16
- second window built-in · modal **hand-rolled overlay** for custom content / **OS window-modal sheet** for button prompts (`window.prompt` → `beginSheetModalForWindow:`) · parent blocked hand-rolled (byte-identical before/after PNGs) · focus return hand-rolled · shared state built-in · ping/pong built-in · theme-all-windows built-in · parenting **not-achievable** (`WindowKind::Floating` documents parenting the macOS backend does not implement; refuted by z-order and minimize) · close veto **built-in** · quit semantics assembled · native confirm **built-in** · persistence assembled · multi-monitor not-verified · shortcuts assembled
- traps: `WindowOptions.window_bounds` is a content rect but `window.bounds()` is a frame rect — round-tripping grows every window 32 px per launch (480→512→544); element-level `.on_action()` never fired for ⌘W while global `cx.on_action` did.

ledger: 5,510,448 B · LoC 1521 (main 1381 + cell 140; 212 self-test) · zero helper crates (money as `i64` minor units) · selftest 22/22
- numeric field hand-rolled · locale hand-rolled · tabular figures **built-in** (`FontFeatures` — measured: `'1'×10` = 63.44 px vs `'0'×10` = 86.68 px on the system font; 86.68 for both with `tnum`+`lnum`) · validation hand-rolled · dropdown hand-rolled (`deferred(anchored())`, no z-index) · date hand-rolled mask · slider↔field assembled · tab order built-in · Enter-moves-down assembled · undo form-level hand-rolled (field-level absent) · live totals built-in · TSV built-in clipboard + hand-rolled format · a11y **not-achievable** (AX tree = 4 elements: traffic lights + title; no accesskit/NSAccessibility anywhere in gpui 0.2.2) · IME not-achievable as built
- traps: `on_key_down` on a cell and on the root both fire in bubble phase (Tab advanced twice); gpui synthesises `ClickEvent::Keyboard` for Space/Enter on focusable clickables (double-fire if you also handle the key).

## iced (=0.14.0)

windows: build 32.4 s · 10.8 MB · LoC 1279 (1087 non-comment; ~130 self-test, ~90 objc2, ~60 persistence) · selftest 18/18
- second window / singleton / close+quit semantics **built-in** (`iced::daemon`, `window::open/close/gain_focus`, `exit_on_close_request: false` + `close_requests()`) · modal **hand-rolled OS window-modal**: iced 0.14 has no modality or parenting API (no parent/owner in `window::Settings`), but `iced::window::run` hands a `&dyn Window` on the main thread → ~30 lines of objc2 give a real `NSWindow beginSheet:` (6 synthetic clicks on the parent changed nothing); fallback level: `enable_mouse_passthrough` + `AlwaysOnTop`; `opaque()` is the iced-only mouse-but-not-keyboard level · parenting **hand-rolled** `addChildWindow:` (moving the parent moved the child by the same delta) · shared state + ping/pong **built-in, free** (one `State`, one `update`, no second loop) · theme-all-windows built-in (`.theme()` → `Option<Theme>`) · close veto **built-in** · native confirm assembled (rfd, 3-button NSAlert) · persistence assembled (Moved/Resized → JSON → `Position::Specific`, exact round-trip) · multi-monitor not-verified (`Rescaled` logged) · shortcuts built-in
- traps: `beginSheet:` on a parent that owns a child window **destroys the child on dismissal** (fixed by detaching/re-attaching); widget operations run against every window, so `focus_next()` tabs out of a dialog.

ledger: build 22.5 s · 10.8 MB · LoC 1435 (1226 non-comment) · selftest 29/29
- numeric input **assembled** (controlled `text_input` ⇒ real keystroke filtering) · locale hand-rolled · decimal alignment assembled but **font features not-achievable** (`iced::Font` has no OpenType feature list) · validation assembled (`Save (2 errors)` + summary) · dropdown type-ahead **built-in** (`combo_box`) · date hand-rolled mask · slider↔field built-in one way / hand-rolled for keys · **tab order across mixed controls hand-rolled — iced cannot do it**: only `text_input`/`text_editor`/`scrollable` are focusable; `button`/`checkbox`/`slider` are not and `combo_box` has no `.id()` · Enter-moves-down assembled · undo field-level **not-achievable** (no undo stack in `text_input`), form-level hand-rolled · live totals built-in · TSV ⌘⇧C/⌘⇧V (⌘C/⌘V claimed by `text_input`) · a11y **not-achievable, verified** (AX dump = traffic lights + title; no AccessKit in iced 0.14's manifests) · IME not-verified
- traps: `operation::focus` on a missing id silently no-ops (needs `focusable::unfocus()`); `find_focused`/`unfocus` only reachable behind iced's `advanced` feature.
- evidence caveat: shared desktop contention — items not drivable through it (Ping screenshot, compact-rows toggler, tail of the tab-order walk) are labelled self-test, not synthetic-input.

## egui / eframe (=0.35.0)

windows: build 26.1 s · 12,895,440 B · LoC 732 + selftest 165 · deps rfd =0.17.2, serde/serde_json · selftest 13/13
- second window **built-in** (deferred viewports; singleton is the default since a window is a `ViewportId`) · modal **built-in overlay** (`egui::Modal`); real OS sheets only via `rfd::set_parent(&Frame)` → `beginSheetModalForWindow:` · parent blocked built-in (5 CGEvent clicks on Delete under the modal → 0 trace lines, rows unchanged) · focus return n/a (never an OS window) · cross-window modality hand-rolled (`Memory::focus` is a `ViewportIdMap`, so a modal is per-viewport; children need `ui.disable()`) · shared state **hand-rolled `Arc<Mutex<_>>`** (deferred callback is `Fn + Send + Sync + 'static`) · ping/pong assembled (`request_repaint_of`) · theme-all-windows built-in (one `Context`) · parenting **not-achievable** (no parent/owner/transient in `ViewportBuilder`; only a parent-relative offset) · close veto built-in · quit semantics built-in · native confirm assembled (rfd; AX shows `sheet 1 of window 1`) · persistence **hand-rolled JSON** (eframe's `persist_window` needs a non-default feature and stores one window) · multi-monitor not-verified · shortcuts built-in (per-viewport `InputState`)

ledger: build 49.1 s · 12,872,336 B · LoC 769 + model 295 + selftest 295 · deps egui_extras =0.35.0, rust_decimal =1.39.0 · selftest 26/26 (real key events via `App::raw_input_hook`)
- numeric field hand-rolled on `TextEdit` (`DragValue` can't do ⇧×10 or reject a keystroke) · locale hand-rolled · decimal alignment assembled, font-feature request **not-achievable** (epaint shapes with HarfRust but calls `shaper.shape(buf, &[])`) · validation assembled · combo built-in, type-ahead hand-rolled (by-construction) · date hand-rolled mask · slider↔field **built-in** (zero glue) · tab order built-in · Enter-down hand-rolled · undo field built-in + form hand-rolled · live totals built-in · TSV assembled · a11y **built-in** (225-node tree) · IME not-verified
- findings: `Response::has_focus()` is false whenever the OS window isn't frontmost (silently disables key handling in scripted runs); ⌘C/⌘V arrive as `Event::Copy`/`Paste`, never as key presses; **the spec's `osascript … entire contents` probe returns a false negative for egui** — only a direct `AXUIElement` walk (`apps/egui-ledger/evidence/axdump.swift`) shows the AccessKit tree.

## tauri (=2.11.5)

windows: build 37.5 s · 9,276,400 B · 446 packages (baseline tauri-app 418) · LoC main 801 (677 code, ~150 self-test) + build.rs 6; UI 86 HTML / 147 JS / 67 CSS · plugins: dialog, window-state · selftest 15/15
- second window built-in · modal **assembled → real OS window-modal** · parent blocked **built-in** (3 gated CGEvent clicks on Delete landed on the disabling sheet, no handler fired, 6 rows unchanged; control click without the modal fired) · focus return built-in · shared state built-in · ping/pong built-in · theme-all-windows built-in · parenting **built-in** · close veto built-in · quit semantics built-in · native confirm assembled (dialog plugin / rfd sheet) · persistence assembled (window-state plugin + 3-line session JSON) · multi-monitor not-verified · shortcuts built-in
- headline: **Tauri has OS window-modality — it is spelled `set_enabled(false)`**: on macOS tauri-runtime-wry attaches an invisible alpha-0.5 NSWindow with `beginSheet:` (Windows `EnableWindow`, Linux `set_sensitive`). Traps: that sheet is parent-sized and outranks a dialog placed over the parent; `always_on_top` is **mutually exclusive with `parent`** on macOS (`addChildWindow:` resets the child's level) — the inspector keeps `.parent()`, the modal trades it for mouse reachability; AppKit shifts the parent so the sheet fits. Esc closes the focused rather than the topmost window.

ledger: build 57.0 s · 9,301,184 B · 505 packages (+87 for rust_decimal + clipboard plugin) · LoC main 536 (459 code) + build.rs 6; UI 48 HTML / 213 JS + 123 JS self-test / 61 CSS · selftest 29/29
- numeric field hand-rolled · locale format **built-in** (`Intl.NumberFormat` + `formatToParts()` supplies the separators the keystroke filter uses — no icu/num-format dep) / parse hand-rolled · tabular figures **built-in** (one CSS line) · validation hand-rolled · dropdown built-in · date **built-in** (native segmented control; displays in the *system* locale, unaffected by the app toggle) · slider↔field assembled · tab order built-in (19 real Tab keys logged across 4 rows) · Enter-moves-down hand-rolled · undo both (WebKit field-level + Rust form-level) · live totals built-in · TSV assembled · a11y **built-in** (654-element dump; `aria-label` alone yields real roles: pop up button, slider, incrementor) · IME not-verified
- rejected: `<input type="number">` (no typing filter, locale-locked to `.`). Evidence caveat: clicks gated on a CoreGraphics "who owns this point" probe because of sibling agents; shared clipboard.

## dioxus-native (Blitz main 64eb278 / 0.3.0-beta.2, dioxus =0.7.9 facade with default-features=false)

**Sharing verdict: SHARED, zero diff hunks, both apps.** `dioxus = { version = "=0.7.9", default-features = false, features = ["macro","html","hooks","signals"] }` resolves to the same dioxus-core/-html/-signals/-hooks 0.7.10 that dioxus-native uses; both `main.rs` files are `#[path = "../../dioxus-<spec>/src/app.rs"] mod app;` plus their own `platform.rs`. The desktop `app.rs` files (807 / 935 lines) compile and run unmodified (first `cargo check`: 0 errors for ledger, 2 for windows — `Runtime`/`current_scope_id` live in `dioxus::core`, not the prelude).

windows: clean build 76.5 s (vs 35.0 s desktop) · 24,533,888 B (vs 6,477,664) · 520 unique crates (vs 377, same `cargo tree -e normal --prefix none | sort -u | wc -l` command; the desktop agent's 291 used a different count) · idle RSS 107–113 MB (vs 94) · **1 process** (vs 1 + 3 WebKit XPC) · own LoC main+platform 458 (vs 258) · selftest 13/13
- multi-window: **not possible through the public launcher, possible if you become the application** — `dioxus_native::launch_cfg` hard-codes one `pending_window`; `DioxusNativeApplication::add_window` is public but unreachable from a component and its windows get no context injection and no `initial_build()` (render blank). `platform.rs` re-implements `launch_cfg` (446 L) over `blitz-shell`'s `create_default_event_loop`/`BlitzShellProxy`/`BlitzApplication`/`View`/`WindowConfig` + `DioxusDocument`, draining a thread-local queue of pre-built VirtualDoms in `can_create_surfaces`/`proxy_wake_up`. Result: 4 windows, count 1→2→3, singleton focus, `with_root_context` Signal sharing identical to the webview, and a **genuine `CloseRequested` veto** (better than dioxus-desktop 0.7.9).
- not-achievable: OS window parenting (none in winit 0.31-beta), OS modality (in-app scrim only — verified blocking a Delete click, byte-identical PNGs), ⌘-chords (**never reach the VirtualDom**: AppKit standard key bindings dropped by dioxus-native-dom; no menubar, so no ⌘W), `prefers-color-scheme` follows the document's `color-scheme`, not the OS.

ledger: clean build 75.2 s (vs 54.4) · 27,858,960 B (vs 6,424,992) · 524 crates (vs 380) · idle RSS 112 MB (vs 88–98) · 1 process (vs 4) · own LoC 78 (vs 65) · selftest 26/26
- Blitz gaps (evidenced): `<select>`, `<input type=date>`, `<input type=range>` are in the DOM and the AccessKit tree but **paint nothing** (3 rows → not-achievable); checkbox/radio dispatch only `input`, never `change` (`blitz-dom/events/pointer.rs:645-695`) and `FormData::checked()` is always false; Tab/⇧Tab move nothing and `set_focus_to` never calls `generate_focus_events` (programmatic focus and Tab emit no focus/blur; pointer-driven focus/blur do fire — blur-normalise → `1,234.50` verified); `tabindex="-1"` = non-focusable (`flush_is_focussable`); IME `DomEventData::Ime(_) => None` upstream; ZWJ family emoji renders blank (CJK 世界 and Arabic مرحبا shape correctly).
- a11y: 252-node AccessKit tree with correct roles (37 text fields, 12 pop-up buttons, 12 checkboxes, 1 slider) but `AXTitle`/`AXValue` empty — `aria-label`s and values don't reach the OS; first `entire contents` request returns only chrome (lazy activation).
- working well: Stylo handled every selector this corpus uses (grid, sticky, fixed scrims, `color-mix`, `tabular-nums`), ancestor-class restyle, live `value` attribute updates.

## slint (=1.17.1)

windows: build 50.1 s · 16,572,496 B · 327 crates · LoC app 985 (main.rs 494, main.slint 404, mac.rs 84, build.rs 3) + 474 harness · deps rfd =0.15.4, raw-window-handle =0.6.2, objc2 =0.5.2 (macOS), slint feature `raw-window-handle-06` · selftest 19/19
- second window built-in · modal **hand-rolled → real OS window-modal** (`-[NSWindow beginSheet:]` via `window_handle()`) · parent blocked built-in (CGEvent click on Delete with the sheet up produced no delete; identical click without it did) · focus return built-in · shared state **assembled** (one `ModelRc`) · ping/pong assembled · theme-all-windows **hand-rolled** · parenting hand-rolled (`addChildWindow:`) · close veto built-in · quit semantics built-in · native confirm assembled (rfd) · persistence assembled · multi-monitor not-verified · shortcuts hand-rolled
- findings: **`export global` is per-component-instance** — two windows get two `Palette`s; the only real cross-window signal is a shared `ModelRc` (a one-row `VecModel` here). **Slint remaps modifiers on macOS: `modifiers.control` IS Command** (`meta` is Control), undocumented. `Window::position()/size()` are physical but `scale_factor()` reads 1.0 until mapped → a naive persistence round-trip reopens at 2× size. Sheet placement centred, not sliding from the titlebar; Esc inside the sheet needs a prior click.
- **harness trap:** `osascript … keystroke "w" using command down` delivers no modifier flags to winit (`ctrl=0 meta=0 alt=0 shift=0`); modifier shortcuts need a CGEvent that also posts `flagsChanged` (25-line Swift helper, invocation in `evidence/log.txt`). Earlier corpus results claiming "shortcut doesn't work" from `osascript keystroke … using` are suspect.

ledger: build 54.9 s · 17,959,040 B · 327 crates · LoC app 976 (main.rs 451, main.slint 363, num.rs 159, build.rs 3) + 472 harness · deps arboard =3.6.1 · selftest 19/19 (via `try_dispatch_event`)
- numeric field **assembled** (`TextInput.key-pressed` returning `accept` is a genuine pre-insertion filter; `input-type: decimal` exists but rejects grouping separators) · locale hand-rolled · decimal alignment hand-rolled, font features **not-achievable** (no property anywhere) · validation assembled · combobox type-ahead assembled · date assembled mask + built-in picker · slider↔field **built-in** · tab order **built-in** · Enter-moves-down assembled · undo built-in (field) + assembled (form) · live totals built-in · TSV assembled (arboard; ⌘⇧C/⌘⇧V) · a11y **built-in, verified** (49 text fields / 12 pop-ups / 12 checkboxes / slider, each labelled; first `entire contents` returns only chrome — AccessKit activates lazily, dump twice) · IME not-verified
- evidence caveat: the live CGEvent keystroke run was abandoned after three attempts (sibling agents' always-on-top dialogs stole key focus); mouse-driven checks retained (`live-locale-fr-FR.png`).

## floem (git 778bb5f2)

windows: build 48 s · 18.4 MB · LoC 1162 (952 code; ~190 self-test + shared-desktop hooks) · deps rfd =0.17.2, objc2 =0.6.3, raw-window-handle =0.6.2 · selftest 21/21
- second window **built-in** (focus-existing hand-rolled) · modal **OS window-modal assembled from objc2** (`-[NSWindow beginSheet:]`) · parent blocked built-in to the sheet (6 clicks on Delete while the sheet was up → 0 presses; control click on the sheet's Cancel worked; the same Delete click fired once the sheet closed) · focus return hand-rolled · shared state **built-in** (process-global signals) · ping/pong built-in (no wake needed) · theme-all-windows **built-in** (`set_global_theme`) · parenting not-achievable in floem, assembled via `addChildWindow:` · close veto **built-in** · quit semantics assembled · native confirm assembled (rfd) · persistence **hand-rolled** · multi-monitor not-verified · shortcuts assembled
- verified: window count 1→2→3; ⌘W on the focused window, Cancel→alive, Discard→`EXIT_CODE=0` with real ⌘W keystrokes; persistence by CGEvent title-bar drag → quit → relaunch, byte-exact for both windows.
- upstream-worthy: `bounds_of_content_on_screen()` returns **window-relative** coordinates on macOS (winit `surface_position()`), so naive geometry persistence creeps by the title-bar height.

ledger: build 81 s · 18.3 MB · LoC 1108 (941 code) · deps rust_decimal =1.39.0 only · selftest 37/37
- numeric field hand-rolled · locale hand-rolled · decimal alignment assembled, font features **not-achievable** · validation assembled · dropdown built-in, type-ahead hand-rolled · date hand-rolled mask · slider↔field **built-in** · tab order **built-in but not reading order** (visits a row's `Dropdown` one row late) · Enter-moves-down hand-rolled · undo field-level not-achievable (`TextInput` has none; Lapce's `text_editor` widget does) / form-level hand-rolled · live totals built-in · TSV built-in clipboard (`floem::Clipboard`) · a11y **not-achievable** · IME not-verified
- verified: `1234.5` + Tab → `1,234.50` with totals recomputed; locale toggle → `1 234,50` live; alignment crop; Tab-order walk logged both directions; `FILTER "a4" -> "4"`; validation screenshot; AX dump.
- evidence note: ~10 sibling GUI apps were on the display; both apps carry a documented verification-only raise hook; two small Swift helpers (`clickpid.swift`, `dragwin.swift`) live in evidence/.

## vizia (=0.4.0)

windows: build 50.4 s · 23,199,984 B · LoC 1021 (~632 production, 141 self-test/pose, 59 CSS) · deps rfd =0.17.2 · selftest 17/17
- second window **built-in** · modal **assembled** (in-framework focus lock + always-on-top, not OS window-modal on macOS) · parent blocked assembled (`.disabled()` inherits and kills hover→press; 2 clicks on Delete → no confirm, rows unchanged) · focus return built-in · shared state **built-in** · ping/pong built-in · theme-all-windows assembled · parenting **not-achievable on macOS** (owner/transient is `#[cfg(windows)]`) · close veto **built-in** (models are visited before views on the same entity) · quit semantics built-in · native confirm assembled (rfd; Cancel → alive, No → exit 0) · persistence hand-rolled (move → quit → relaunch → same bounds; `inspector_open=1` reopens it) · multi-monitor not-verified **and broken by construction** (`style.dpi_factor` is a single global) · shortcuts assembled
- window model: a window is a **view** — a `Binding` on a bool creates/destroys the OS window; one Context/tree/signal graph makes cross-window state free.
- defects found in vizia 0.4: `.title()` on a sub-window **renames its ancestors** (`SetTitle` propagates Up un-consumed — `evidence/title-leak.txt`); `WindowEvent::SetEnabled` is a Windows-only stub that *focuses* the parent on macOS; `Window::popup` stores `is_modal: true` regardless of the argument; the built-in theme's `:root` matches only `Entity::root()`, so sub-windows get no background; `Tree::get_parent_window(e)` skips `e` itself.

ledger: build 34.6 s · 24,686,096 B · LoC 1280 (~840 production, 245 self-test/pose, 35 CSS) · deps rust_decimal =1.42.1, chrono =0.4.45 · selftest 30/30
- numeric field hand-rolled (**vizia cannot refuse a keystroke** — `validate` is post-validation) · locale hand-rolled · decimal alignment hand-rolled, font features **not-achievable** (monospace only) · validation **built-in** (`:invalid`) + assembled summary · dropdown type-ahead **built-in** (`ComboBox`) · date assembled (mask + built-in `Calendar`) · slider↔field assembled · tab order built-in · Enter-moves-down hand-rolled · undo form-level only (field-level not-achievable; `Textbox` has no undo stack) · live totals built-in (`Memo`) · TSV built-in clipboard + hand-rolled rows · a11y **built-in, 160-element dump** (first macOS AX query returns an empty tree — probe twice) · IME not-verified
- highest-value trap: **`FocusOut` never emits `Submit`** — so "format on blur" must be driven from the raw focus event. (Verifier correction 2026-08-30: `TextEvent::Blur` *is* emitted via the Textbox click-away listener; what never commits is Tab-away.) Also `Handle::class("a b")` does not split on whitespace.
- evidence note: four sibling apps moved windows over the same coordinates; evidence uses AXRaise+retry clicks, a `CGEventPostToPid` key helper (`evidence/postclick.swift`) and `WINDOWS_POSE`/`LEDGER_POSE` hooks; `evidence/run-evidence.sh` reproduces the SPEC-9 run.

## freya (=0.4.0; transitive freya-* pinned at 0.4.1 to match the cohort — a fresh resolution picks 0.4.3)

windows: build 33.7 s · 21.8 MB · LoC 1251 (~120 self-test) · deps rfd =0.17.2, serde/serde_json, async-io =2.6.0 · selftest 16/16
- second window built-in · modal **hand-rolled** (in-framework overlay over a real child window; no OS modality in Freya/winit) · parent blocked assembled (`rect().interactive(false)`; identical verified click on Delete does nothing with the modal open, pops the NSAlert with it closed) · focus return built-in · shared state **built-in** (`State::create_global` — a `Copy` struct captured by two root closures) · ping/pong built-in · theme-all-windows built-in · parenting assembled (see crash) · close veto assembled · quit semantics assembled (`RendererContext::exit()`) · native confirm assembled (rfd) · persistence assembled (AX-move → quit → relaunch at the same frame) · multi-monitor not-verified · shortcuts hand-rolled
- headline trap: winit's `with_parent_window` **crashes Freya** ("AccessKit adapter must be created before the window is shown") because `addChildWindow:` orders the child in before Freya builds the adapter — worked around with 20 lines of `objc_msgSend` (no new crate). Also `with_on_close` is `Send` so it cannot read a `State`; no way to post a `CloseRequested`; `WindowConfig` has no `with_position`.

ledger: build 64.8 s · 23.3 MB · LoC 1490 (~160 self-test, ~90 locale parser/formatter) · deps rust_decimal =1.39.0, async-io =2.6.0 (no arboard — `Clipboard` is in the prelude) · selftest 22/22
- numeric field **assembled** (`on_validate` is a true keystroke filter that undoes rejected input; stepping hand-added) · locale hand-rolled · decimal alignment / font features hand-rolled (no font-feature API) · validation assembled · dropdown type-ahead hand-rolled (`Select` has none) · date assembled (masked Input) · slider↔field assembled (Slider is percentage-only, 4 % fixed key step) · tab order built-in · Enter-moves-down assembled · undo built-in (field, ⌘Z/⌘Y) / assembled (form, ⌘⇧Z) · live totals built-in · TSV built-in · a11y **built-in but off by default** (331-element dump; the tree is lazily activated — `System Events` returns only the title bar until `AXManualAccessibility` is set) · IME not-verified
- findings: `Input::text_align(TextAlign::Right)` is **broken** (text pushed out of the internal ScrollView; a `1` renders empty) — alignment done with a monospaced family + space padding; `Input` swallows every key it doesn't use (kills app shortcuts; third Freya app to hit this), has no `on_blur` (built on `Platform::focused_accessibility_id`); `Checkbox` cannot be given an accessible name; editor redo is ⌘Y not ⌘⇧Z; ⌘C/⌘V and form undo re-bound to ⌘⇧ variants.
