{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window. core:default includes core:event:default, which the frontend needs to `listen` for the Rust-emitted `tick` events. App-defined commands (get_config/set_rate/set_paused/report_stats) do not require permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
