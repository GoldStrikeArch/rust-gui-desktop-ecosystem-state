{"default":{"identifier":"default","description":"All four windows. core:default covers the event bus (listen/emit) that carries the shared model to every window. Both plugins (dialog, window-state) are driven exclusively from Rust, which bypasses the ACL entirely, so neither needs a permission entry. App-defined #[tauri::command]s never need permissions.","local":true,"windows":["main","inspector","prefs","edit"],"permissions":["core:default"]}}