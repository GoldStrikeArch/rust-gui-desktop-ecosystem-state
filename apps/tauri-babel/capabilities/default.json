{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: core defaults only. App-defined commands (get_corpus/get_flags/report) do not require permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
