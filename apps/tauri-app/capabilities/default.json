{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: core defaults only (events, window/webview introspection). App-defined commands (add_task/delete_task/get_tasks) do not require permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
