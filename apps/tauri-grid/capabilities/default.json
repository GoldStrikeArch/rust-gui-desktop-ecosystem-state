{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core defaults for the main window. App-defined commands (get_meta/get_rows/set_filter/set_sort/report) do not require permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
